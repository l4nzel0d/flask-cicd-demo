[pytest]
testpaths = tests
# Корень проекта добавляется в sys.path: иначе тесты не находят модуль app,
# так как pytest кладёт в путь только каталог самого теста.
pythonpath = .
junit_family = xunit2
addopts = -ra
